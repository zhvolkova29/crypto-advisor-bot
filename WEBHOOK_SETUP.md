# Настройка Webhook для Telegram бота на Render

## Что изменилось

Бот теперь использует **webhook** вместо polling. Это более надежный способ для production среды на Render.

## Шаги для настройки

### 1. Обновите код на Render

Закоммитьте и отправьте изменения в репозиторий:

```bash
git add render_web.py
git commit -m "Switch to webhook mode"
git push
```

Render автоматически перезапустит сервис после получения изменений.

### 2. Добавьте переменную окружения WEBHOOK_URL

В панели Render:

1. Откройте ваш сервис
2. Перейдите в раздел **Environment**
3. Добавьте переменную окружения:
   - **Key**: `WEBHOOK_URL`
   - **Value**: `https://your-service-name.onrender.com/webhook`
     (замените `your-service-name` на имя вашего сервиса на Render)

### 3. Установите webhook

После деплоя откройте в браузере:

```
https://your-service-name.onrender.com/set-webhook
```

Или используйте curl:

```bash
curl https://your-service-name.onrender.com/set-webhook
```

Вы должны увидеть ответ:
```json
{
  "status": "success",
  "message": "Webhook установлен",
  "url": "https://your-service-name.onrender.com/webhook",
  "result": true
}
```

### 4. Проверьте работу бота

Откройте Telegram и отправьте боту команду `/start`. Бот должен ответить!

## Проверка статуса

### Проверить, что webhook установлен:

Откройте в браузере:
```
https://your-service-name.onrender.com/health
```

Должен вернуться ответ:
```json
{
  "status": "healthy",
  "bot_initialized": true
}
```

### Проверить логи на Render:

1. Откройте ваш сервис на Render
2. Перейдите в раздел **Logs**
3. Убедитесь, что видите сообщения:
   - `🚀 Инициализация интерактивного бота...`
   - `✅ Бот инициализирован и готов к работе через webhook`
   - `🔗 Установка webhook: https://...`
   - `✅ Webhook установлен`

## Устранение неполадок

### Бот не отвечает

1. Проверьте, что webhook установлен:
   ```
   https://your-service-name.onrender.com/set-webhook
   ```

2. Проверьте логи на Render на наличие ошибок

3. Убедитесь, что переменная `WEBHOOK_URL` установлена правильно

4. Проверьте, что сервис работает (статус "Live" на Render)

### Webhook не устанавливается

1. Убедитесь, что сервис полностью задеплоен (статус "Live")

2. Подождите 1-2 минуты после деплоя перед установкой webhook

3. Проверьте логи на Render на наличие ошибок

4. Попробуйте установить webhook вручную через endpoint `/set-webhook`

### Удалить webhook (для тестирования)

Если нужно вернуться к polling:

```
https://your-service-name.onrender.com/delete-webhook
```

## Преимущества webhook

- ✅ Более надежная работа в production
- ✅ Нет постоянного polling (меньше нагрузка)
- ✅ Быстрая обработка сообщений
- ✅ Лучше для Render Web Service

## Автоматическая установка webhook

Webhook автоматически устанавливается при старте сервиса (если установлена переменная `WEBHOOK_URL`). Но можно установить вручную через endpoint `/set-webhook` для надежности.

